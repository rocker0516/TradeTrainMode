# 🚀 訓練快速參考

## 💡 核心改變

### 總步數計算公式
```
總訓練步數 = vec_envs × episodes × episode_steps
```

### 必需參數
- ✅ `--episodes`：每個環境的回合數（**必需**）
- ✅ `--episode_steps`：每回合的步數（**必需**）
- ❌ `--total_timesteps`：已移除

---

## 📋 基本使用

```bash
python Train/train.py \
  --start_date 20230101 \
  --end_date 20230331 \
  --vec_envs 16 \          # 並行環境數
  --episodes 100 \         # 每環境100回合 ✅ 必需
  --episode_steps 500      # 每回合500步 ✅ 必需

# 自動計算：16 × 100 × 500 = 800,000 步
```

---

## 🎯 常用配置

### 快速測試（1分鐘）
```bash
--vec_envs 2 --episodes 10 --episode_steps 50
# = 1,000 步
```

### 標準訓練（30分鐘）
```bash
--vec_envs 8 --episodes 100 --episode_steps 500
# = 400,000 步
```

### 大規模訓練（4小時）
```bash
--vec_envs 16 --episodes 500 --episode_steps 1000
# = 8,000,000 步
```

---

## 📊 訓練規模規劃

| vec_envs | episodes | episode_steps | 總步數 | 預估時間 |
|----------|----------|---------------|--------|----------|
| 2 | 10 | 50 | 1K | 1分鐘 |
| 4 | 50 | 500 | 100K | 10分鐘 |
| 8 | 125 | 500 | 500K | 45分鐘 |
| 16 | 125 | 500 | 1M | 1.5小時 |
| 16 | 500 | 1000 | 8M | 10小時 |

---

## ⚙️ 參數建議

### vec_envs（環境數）
- **調試**：1-4
- **標準**：8-16
- **大規模**：32+

### episodes（回合數）
- **快速實驗**：10-50
- **標準訓練**：100-200
- **深度訓練**：500+

### episode_steps（步數/回合）
- **短期策略**：50-100（4-8小時）
- **日內策略**：288（24小時）
- **中期策略**：500-1000（2-3天）

---

## 📈 輸出範例

```
============================================================
訓練配置摘要
============================================================
並行環境數：16
每環境回合數：100
每回合步數：500
總回合數：1,600 (= 16 × 100)
總訓練步數：800,000 (= 16 × 100 × 500)
============================================================
```

---

## 🔍 完整參數列表

```bash
python Train/train.py \
  --csv Data/BTCUSDT_futures_volume_5years_5min.csv \
  --start_date 20230101 \           # 必需：起始日期
  --end_date 20230331 \             # 必需：結束日期
  --vec_envs 16 \                   # 並行環境數（默認16）
  --episodes 100 \                  # 必需：每環境回合數
  --episode_steps 500 \             # 必需：每回合步數
  --window_size 288 \               # 觀察窗口（默認288）
  --initial_balance 10000 \         # 初始資金（默認10000）
  --leverage 5 \                    # 槓桿倍數（默認5）
  --position_scale 0.5 \            # 倉位上限（默認0.5）
  --transaction_fee 0.001 \         # 手續費率（默認0.001）
  --min_trade_amount 10 \           # 最小交易額（默認10）
  --logdir training_results/my_train
```

---

## 📚 更多資訊

詳細說明請參考：
- 📄 [訓練步數重構報告](reports/training_timesteps_refactor.md)
- 📄 [訓練執行指南](reports/train_execution_guide.md)
- 📁 [訓練範例腳本](examples/train_example.sh)

---

**更新日期**：2025-10-03  
**版本**：v2.0

